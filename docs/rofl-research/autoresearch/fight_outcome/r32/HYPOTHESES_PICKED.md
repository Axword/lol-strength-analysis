# R32 HYPOTHESES_PICKED — check03-share (wave2)

**Suite:** Path1 `2970132-g1-holdout` · product `cusum_engage_then_skills` · R30 defaults (`idleFollowActual`, `aaAtEachMark=false`, R-pulse=0)  
**Owns:** check03 Olaf→Camille multi-caster share without R26 global/assist one-coeff S1 trap  
**Does not own:** Galio miss-kill (R24/R31+) · one-coefficient early+late  

`false_all_in` / `early_poisoned` = diagnostics only. FA ≠ odds.

## Auto-picked

| # | Hypothesis | Status |
|---|------------|--------|
| H1 | **Residual HP share:** measure `actualDrop/modelDrop` at probe firstLethal; gate on extreme early lethal (≥8s), not ally skill count — lifts Path1 c3 without S1 c2 collision. | **KEEP** |
| H2 | **Opener-only residual:** apply share only until probeLethal+pad; finish marks share=1. | DISCARD (c3 re-poisons) |
| H3 | **R26 assist-gated recreate:** allyMin=5 + share=0.20 clears c3 but steals S1 Cass→Viktor. | DISCARD product (ablation confirms trap) |
| H4 | **Lower earlyLethal gate (6s):** same as H1 on these suites. | redundant vs H1 |

## KEEP bar

Product KEEP iff **S0 FA↑** and **S1 not regress**. Met by H1 / e5.
