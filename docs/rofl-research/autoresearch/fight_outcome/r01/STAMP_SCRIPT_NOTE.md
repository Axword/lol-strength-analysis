# Stamp script location

`scripts/stamp_digest_path1_provenance.py` lives in worktree
`/Users/river/.codex/worktrees/rofl-fo-r01` (branch `adv/fo-r01-digest-path`).

Parent checkout code was not edited (`never_edited_parent: true`).
Merge via PARENT_MERGE when KEEP.
