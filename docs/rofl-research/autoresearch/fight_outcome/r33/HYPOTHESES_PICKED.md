# R33 HYPOTHESES — pathmae-c2

Auto-picked from `program.md` + GOAL BOTH + R30 handoff. **No human choice.**  
Mandate: lift **c2_burst pathBand / pathMae** (Olaf→Trundle) without S1 regress and without reopening FK.  
R30 closed lethErr (2.81→0.33); residual `maeHpBurst≈117.7` vs cap 90 (`pathOk=false`).

Product KEEP only if **S0 FA↑** AND **S1 flat+**. Selector stays `cusum_engage_then_skills`.  
FA ≠ odds / pBlue%.

## Queue (advance on failure mode)

1. **H1 Baseline remesaure** — R30 KEEP defaults; confirm c2_burst pathMae residual; classify opener vs finish (earlyMae already ≤50 → finish/path track).
2. **H2 Soften Q/E pulses (FINISH path)** — Slightly shorter Q/E slots so model HP tracks truth longer without missing lethErr≤0.75; R stays 0.
3. **H3 Bound finish AA** — Lower `finishAaMax` / optional finish window to cut post-mark overdamage pad (path only; keep lethOk).
4. **H4 Killer share scale** — Uniform killer `share<1` via pulse/share (ally assist disclosed; not invent); stop if miss-kill or FK.
5. **H5 Ally-truth pulses** — `--ally-truth` with modest ally share so Olaf alone does not eat whole path MAE (assistProbe ally pid4).
6. **H6 Combo KEEP candidate** — Best path combo that lifts S0 FA without S1 regress / FK reopen; else **NO KEEP** + sharper blocker.

Deferred: Galio miss-kill (R24/R31+); Camille earlyPoisoned (R26); mark/density/regen sweeps; invent pins; near_hp_drop.
