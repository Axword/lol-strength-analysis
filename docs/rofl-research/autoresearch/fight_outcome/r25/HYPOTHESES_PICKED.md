# R25 HYPOTHESES — path1-remeasure

Auto-picked (no human choice). Remeasure-only; no product code KEEP expected.

1. **H1 Remeasure authoritative S0 Path1 FA on 2970132-g1** (KEEP measure)
   - After parent merge of R19 `idleFollowActual: true` into product defaults.
   - Selector: `cusum_engage_then_skills` (non-drop). Product dense: `--dense-window 1.0 --dense-max 1 --mark-min-gap 0.4`.
   - Compare vs R05 0.228 and R19 claim 0.411.

2. **H2 Score S1 under current law** (KEEP measure; no tune)
   - Suite `2970137-g1-holdout` via `fight_agreement_suite --suite-label S1`.
   - No S1 tuning; freeze current-law FA + passRate + failing-window audits.

3. **H3 Sharpen blocker from remesaure** (KEEP disclose)
   - If S0 FA ≈ R19 0.411 → idle-follow KEEP holds; residual is miss-kill / earlyPoisoned / finish (R18 handoff), not idle honesty.
   - If S0 FA regresses toward R05 0.228 → remeasure exposes merge/seed or scorer drift; document delta before any KEEP claim.

Deferred: product code KEEP (remeasure-only); near_hp_drop; invent HP/combat/ranks; S1 tuning.
