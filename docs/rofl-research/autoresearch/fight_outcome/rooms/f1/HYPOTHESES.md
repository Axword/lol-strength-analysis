# F1 Hypotheses (auto-pick R01–R03)

1. **Single digest CLI** — Document one `npm`/`python` chain: Path1 final rfc461 → `jsonl_to_timeline.py` → validate `--product` living_post_seed_v1 on 2970132.
2. **Source preserve** — Audit merge/fuse for silent wipe of `hpSource`/`combatSource`/`abilityRanksSource`; patch strip bugs in worktree only.
3. **DIGEST.md smoke** — Exact commands + sha256/sizes for events+timeline; macOS runnable; no rematch/rsync footguns.
4. **Identity bind** — Prove PUUID/Riot ID → netId → pid; fail-closed on scramble.
5. **Second host doc** — Prefer also document 2970110/holdout digest if pins allow; else disclose gap (no invent).
6. **Failure modes** — Catalog clear errors when PE seed missing vs invent temptation.
